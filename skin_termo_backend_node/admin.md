# SkinTermo Admin Credentials

The System Administrator account has been generated successfully and stored securely inside the PostgreSQL `skintermo` database.

You can use the following credentials to log in to the backend or React frontend dashboards as an administrator.

### Credentials

- **Email:** `admin@skintermo.com`
- **Password:** `admin123`
- **Role:** `admin`

### Usage

1. Go to your frontend web application (`http://localhost:5173` or similar Vite port).
2. Enter the email and password above.
3. Because the user is tagged as `admin`, the backend will route privileges correctly allowing you complete access over the Patient and Doctor management APIs.
